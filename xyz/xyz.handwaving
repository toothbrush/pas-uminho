process Client:
    can make then receive request

process Server: (is a filter)
    filter 1: full-media answer
    filter 2: text-only  answer
    filter 3: decide which of filter{1,2} will answer

process LoadBalancer: (is a filter)
    can receive requests then forward an answer
    decides which Server will answer
    should probably handle dynamic creation of up to N web servers
